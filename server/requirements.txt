flask==2.3.3
flask-cors==3.0.10
python-dotenv==1.0.0
huggingface-hub==0.24.7  # Latest stable
transformers==4.45.2     # For SpeechT5
torch==2.4.1+cpu         # CPU-only version (important for Render; no CUDA)
torchaudio==2.4.1+cpu    # For audio saving
datasets==3.0.1          # For speaker embeddings download